# Client Configuration
# Copy this file to .env.local and customize as needed

# Server hostname (defaults to window.location.hostname)
# VITE_SERVER_HOST=localhost

# Server port (defaults to 8080)
# VITE_SERVER_PORT=8080

# API base path prefix (defaults to empty)
# Use this when running behind a reverse proxy that routes based on path
# Example: /api (without trailing slash)
# VITE_API_BASE_PATH=/api
