# BounceBot Server Configuration
# Copy this file to .env and modify as needed.
# All values shown are defaults.

# Server port
PORT=8080

# Path to session data file for persistence
DATA_FILE=sessions.json

# Comma-separated list of allowed origins for CORS and WebSocket
# Each hostname allows both http://hostname and http://hostname:port
# Example: localhost,myserver.com,192.168.1.100
ALLOWED_ORIGINS=localhost

# Auto-save interval in seconds
AUTO_SAVE_INTERVAL=30

# Session cleanup interval in seconds (default: 1 hour)
CLEANUP_INTERVAL=3600

# Session max age in seconds before cleanup (default: 24 hours)
SESSION_MAX_AGE=86400

# Player disconnect grace period in seconds
DISCONNECT_GRACE_PERIOD=30
